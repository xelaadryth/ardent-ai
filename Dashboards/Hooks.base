views:
  - type: table
    name: Table
    filters:
      and:
        - type == "hook"
        - status != "completed"
    order:
      - file.name
      - status
    sort:
      - property: status
        direction: ASC
