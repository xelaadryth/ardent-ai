views:
  - type: table
    name: Table
    filters:
      and:
        - type == "hook"
        - status != "completed"
    order:
      - file.name
      - status
      - summary
      - priority
      - leads
    sort:
      - property: status
        direction: ASC
