I want to update all the templates so that the frontmatter contains all the fields needed to re-generate the vault_index.json. Every field that is present in the vault_index.json should be present in the frontmatter. We can deprecate "entities" and "summary" by combining them into "tags", and standardize them to #camel_case. For now fix only the SOUL.md, template files, and the vault_index.json since we'll do a separate pass for more specific files. Since "Summary" is going away, we can rename the markdown section from "Summary" or "Overview" to "Description" to avoid confusion and standardize our templates. Importantly, every link that is in the document should be copied to the frontmatter for easy indexing.